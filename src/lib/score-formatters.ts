export const scoreFormatterLong = new Intl.NumberFormat(undefined,
    {
        notation: "compact",
        compactDisplay: "long"
    });

export const scoreFormatterShort = new Intl.NumberFormat(undefined,
    {
        notation: "compact",
        compactDisplay: "short"
    });